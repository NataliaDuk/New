<?php
function register ($name, $tel, $url, $email)
{$name=trim($name);
    $tel=trim($tel);
    $email=trim($email);
    $url=trim($ul);
    if($name==''||$tel==''||$url==''||$email=='') { $registr="Не введено имя"; return false;}
    }
    ?>
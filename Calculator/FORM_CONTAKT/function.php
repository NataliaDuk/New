<?php
function register ($name, $tel, $url, $email)
{$name=trim(htmlspecialchars($name));
    $tel=trim(htmlspecialchars($tel));
    $email=trim(htmlspecialchars($email));
    $url=trim(htmlspecialchars($ul));
    if($name==''|| $tel==''|| $url==''|| $email=='') { $registr="Не введено имя"; return false;}

    }
    ?>